{
  "name": "mini-agent-monorepo",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @mini-agent/cli dev",
    "typecheck": "pnpm -r typecheck"
  }
}
